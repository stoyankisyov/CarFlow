@model RegionViewModel

<div class="border p-3 mt-4">
    <div class="row align-content-center">
        <h1 class="text-center">@Model.Name</h1>
    </div>

    <table class="table table-bordered table-striped mt-5">
        <thead>
            <tr>
                <th class="text-center" style="font-size:25px">
                    Subregions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var subregion in Model.Subregions)
            {
                <tr>
                    <td>@subregion.Name</td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-action="Index" asp-controller="Region" class="actionButton backButton">Back</a>

</div>