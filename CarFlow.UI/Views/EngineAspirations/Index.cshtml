@model PageViewModel<EngineAspirationViewModel>

<div class="container">
    <div class="row pt-4 pb-1">
        <div class="col-6">
            <h2>
                Engine Aspirations
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" asp-controller="EngineAspirations" class="createButton">Create</a>
        </div>
    </div>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th class="text-center">Name</th>
            <th colspan="2" class="text-center">Actions</th>
        </tr>
        </thead>
        <tbody>
        @if (Model.Content != null)
        {
            foreach (var engineAspiration in Model.Content)
            {
                <tr>
                    <td>
                        <h5 class="text-black text-center mt-3">@engineAspiration.Name</h5>
                    </td>
                    <td>
                        <div class="actionColumn">
                            <a asp-action="Update" asp-controller="EngineAspirations" asp-route-id="@engineAspiration.Id" class="tableButton editButton">Edit</a>
                        </div>
                    </td>
                    <td class="actionColumn">
                        <button type="button" class="tableButton deleteButton" onclick="deleteEngineAspiration(@engineAspiration.Id)">Delete</button>
                    </td>
                </tr>
            }
        }
        </tbody>
    </table>
</div>

<partial name="~/Views/Shared/_Pagination.cshtml" model="Model"/>
<script src="~/js/EngineAspirationRelated/DeleteEngineAspiration.js"></script>