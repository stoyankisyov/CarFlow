@using CarFlow.UI.Enums
@model CarViewModel

<partial name="_CarDetailsPartial"/>
@if (Model.CarType == CarType.ElectricCar)
{
    <partial name="_ElectricCarDetailsPartial"/>
}
@if (Model.CarType == CarType.CombustionEngineCar)
{
    <partial name="_CombustionEngineCarDetailsPartial"/>
}